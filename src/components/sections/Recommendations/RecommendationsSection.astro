---
import SectionContainer from '@components/SectionContainer.astro';
import paulImg from './paul.jpg';
import johnImg from './john.jpg';
---

<SectionContainer id="recommendations">
  <div class="layout">
    <div class="rec1 rec-bg" />
    <img class="rec1 rec-img" src={paulImg} />
    <div class="rec1 rec-text">
      <blockquote>&ldquo;Dallon is a top-tier front end engineer. He possesses fantastic attention to detail, is capable
        of
        thinking through complicated use cases and edge cases, and is happy to share his opinion and speak up in the
        name of the best user experience.&rdquo;</blockquote>
      <div class="author">Paul Clark</div>
      <div class="role">VP of Engineering</div>
    </div>
    <div class="rec2 rec-bg" />
    <img class="rec2 rec-img" src={johnImg} />
    <div class="rec2 rec-text">
      <blockquote>&ldquo;I was always impressed with his attention to detail and ability to find quick, quality
        solutions to the challenges I gave to him. He&apos;s always considering the customer&apos;s / user&apos;s
        needs when looking at
        options and comes prepared with the trade-offs.&rdquo;</blockquote>
      <div class="author">John Smith Jr.</div>
      <div class="role">Product Manager</div>
    </div>
  </div>
</SectionContainer>

<style>
  .layout {
    display: grid;
    grid-auto-flow: row;
    grid-template-columns: none;
    grid-template-rows: auto;
  }

  .rec-img {
    margin-top: var(--size-fluid-3);
    margin-left: auto;
    margin-right: auto;
    block-size: 18rem;
  }

  .rec-text {
    padding: var(--size-fluid-3);
    font-size: var(--font-size-fluid-1);
  }

  .author {
    margin-top: var(--size-fluid-1);
    font-size: var(--font-size-1);
    font-weight: bold;
  }

  .role {
    font-size: var(--font-size-0);
    text-transform: uppercase;
  }

  .rec1.rec-bg {
    grid-row: 1 / span 2;
    grid-column: 1;
    background: var(--gray-7);
  }

  .rec1.rec-img {
    grid-row: 1;
    grid-column: 1;
  }

  .rec1.rec-text {
    grid-row: 2;
    grid-column: 1;
    color: var(--color-text-light);
  }

  .rec2.rec-bg {
    grid-row: 3 / span 2;
    grid-column: 1;
    background: var(--color-bg-tan);
  }

  .rec2.rec-img {
    grid-row: 3;
    grid-column: 1;
  }

  .rec2.rec-text {
    grid-row: 4;
    grid-column: 1;
  }

  @media (--lg-n-above) {
    .layout {
      grid: [start] "image1 text1 text1" min-content [top2] ". . ." var(--size-fluid-2) [bottom1] "text2 text2 image2" min-content [end] / [start] auto [start1] auto [end2] auto [end];
      padding-left: var(--size-fluid-2);
      padding-right: var(--size-fluid-2);
      justify-content: center;
    }

    .rec-img {
      margin: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      min-height: 18rem;
    }

    .rec-text {
      max-inline-size: var(--size-content-2);
    }

    .rec1.rec-bg {
      grid-row: start / bottom1;
      grid-column: start1 / end;
    }

    .rec1 .rec1.rec-img {
      grid-area: image1;
    }

    .rec1.rec-text {
      grid-area: text1;
    }

    .rec2.rec-bg {
      grid-row: top2 / end;
      grid-column: start / end2;
    }

    .rec2.rec-img {
      grid-area: image2;
    }

    .rec2.rec-text {
      grid-area: text2;
    }
  }
</style>