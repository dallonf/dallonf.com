---
import { getCollection } from "astro:content";
import SectionContainer from "@components/SectionContainer.astro";
import CareerSectionInteractive from "./CareerSectionInteractive.svelte";

const jobs = await getCollection("jobs");
---

<SectionContainer id="career" padding={false}>
  <CareerSectionInteractive client:visible client:idle {jobs} />
</SectionContainer>
