---
import { Image } from "astro:assets";
import volusionImg from "./volusion.jpg";
import cmeImg from "./cme.jpg";
import sibiLoginImg from "./sibi-login.png";
import SectionContainer from "@components/SectionContainer.astro";
---

<SectionContainer id="sample-work" bgColor="var(--color-bg-dark)">
  <div class="layout">
    <h2>Sample Work</h2>
    <ul class="sample-work-list">
      <li>
        <Image
          src={volusionImg}
          height={350}
          alt="A screenshot of a Volusion store."
        />
        <h3>Volusion's Page Builder</h3>
        <p>
          Improved performance, maintainability, and test coverage by gradually
          implementing a structured cache system.
        </p>
      </li>
      <li>
        <Image
          src={cmeImg}
          height={350}
          alt="A screenshot of Phunware's Content Management Engine portal."
        />
        <h3>Phunware's Content Management Engine</h3>
        <p>
          Led architecture of a UI revamp, reverse engineering the data model of
          a legacy product.
        </p>
      </li>
      <li>
        <Image
          src={sibiLoginImg}
          height={350}
          alt="A screenshot of a login screen for Sibi."
          style={{ objectPosition: "top" }}
        />
        <h3>Single Sign On for Sibi</h3>
        <p>
          Migrated site-wide authentication to a consistent flow powered by
          Auth0, in order to enable single sign on across multiple applications.
        </p>
      </li>
    </ul>
  </div>
</SectionContainer>

<style>
  .layout {
    background-color: var(--color-bg-dark);
    color: var(--color-text-light);
    text-align: center;
    padding-left: var(--size-fluid-2);
    padding-right: var(--size-fluid-2);
  }

  h2 {
    font-family: var(--accent-font);
    font-weight: bold;
    font-size: var(--font-size-fluid-2);
  }

  .sample-work-list {
    display: flex;
    flex-direction: column;
  }

  .sample-work-list > li {
    font-size: var(--font-size-fluid-0);
    margin-left: auto;
    margin-right: auto;
    margin-top: var(--size-fluid-4);
    max-inline-size: var(--size-header-3);
    text-align: center;
  }

  .sample-work-list > li > img {
    height: var(--size-header-1);
    width: var(--size-header-1);
    object-fit: cover;
  }

  .sample-work-list > li h3 {
    margin-top: var(--size-fluid-1);
    font-family: var(--accent-font);
    font-weight: bold;
  }

  .sample-work-list > li p {
    margin-top: var(--size-fluid-1);
  }

  a,
  a:visited {
    color: var(--color-text-light);
  }

  @media (--lg-n-above) {
    .sample-work-list {
      margin-top: var(--size-fluid-4);
      flex-direction: row;
      gap: var(--size-fluid-4);
      justify-content: center;
    }

    .sample-work-list > li {
      max-inline-size: var(--size-header-2);
      font-size: var(--font-size-0);
      margin: 0;
    }
  }

  @media (--xl-n-above) {
    .sample-work-list {
      gap: 10vw;
    }
  }
</style>
